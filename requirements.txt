# Agent d'analyse de conformite KYC
# Installation : python -m pip install -r requirements.txt
#
# Prerequis : Python 3.10 ou plus recent (syntaxe `str | None` evaluee par Pydantic)

# Modeles de donnees et validation de types
pydantic>=2.0

# Appels HTTP vers Ollama
requests>=2.31

# Parsing des dates et calcul en mois calendaires (relativedelta)
python-dateutil>=2.8

# Interface graphique
streamlit>=1.49

# Tests
pytest>=7.0
